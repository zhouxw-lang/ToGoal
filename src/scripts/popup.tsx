import * as React from 'react';
import * as ReactDOM from 'react-dom';
import PopupPageContainer from './component/PopupPageContainer';

ReactDOM.render(<PopupPageContainer />, document.getElementById('main'));
